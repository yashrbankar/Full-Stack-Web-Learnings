#include<bits/stdc++.h>
using namespace std;

int main()
{
    vector<pair<int, int>> v;
    v.push_back({5,6});
    v.push_back({8,7});
    v.push_back({6,6});
    v.push_back({9,8});
    v.push_back({5,10});
    v.push_back({1,8});
    sort(v.begin(),v.end());
    for()
    {
        
    }

}